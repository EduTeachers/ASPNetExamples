@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>Online Counter</h2>

<p>Count: @ViewData["Counter"]</p>

<a asp-controller="Counter" asp-action="Index" asp-route-id="@((int)ViewData["Counter"] + 1)" class="btn btn-primary">i++;</a>
